listener {
    timeout = 300 # 5 min
    on-timeout = loginctl lock-session # Lock before suspend
}
listener {
    timeout = 330 # 5.5 min
    on-timeout = hyprctl dispatch dpms off # Turn off screen
    on-resume = hyprctl dispatch dpms on
}
